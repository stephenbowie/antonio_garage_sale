import { render, screen } from "@testing-library/react";

describe("ProductView", () => {


  it("display products when initial load and api being called returns data", () => {
   
    expect(true).toBeTruthy();
  });

  it("display products when initial load and api being called returns no data", () => {
    expect(true).toBeTruthy();
  });
});
